<UserControl x:Class="Chronicae.Desktop.Views.StorageManagementView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:local="clr-namespace:Chronicae.Desktop"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <local:EqualityConverter x:Key="EqualityConverter"/>
        <local:NotNullConverter x:Key="NotNullConverter"/>
    </UserControl.Resources>
    
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBlock Grid.Row="0" 
                  Text="Storage Management" 
                  FontSize="28" 
                  FontWeight="SemiBold" 
                  Margin="0,0,0,32"
                  Foreground="{DynamicResource SystemControlForegroundBaseHighBrush}"/>
        
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="300" MinWidth="250"/>
                <ColumnDefinition Width="*" MinWidth="400"/>
            </Grid.ColumnDefinitions>
            
            <!-- Projects Panel -->
            <Border Grid.Column="0" 
                   Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                   CornerRadius="8" 
                   Padding="16" 
                   Margin="0,0,16,0"
                   BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                   BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                              Text="Projects" 
                              FontSize="18" 
                              FontWeight="SemiBold" 
                              Margin="0,0,0,12"/>
                    
                    <!-- Project Actions -->
                    <StackPanel Grid.Row="1" 
                               Orientation="Horizontal" 
                               Margin="0,0,0,12">
                        <Button Content="New Project" 
                               Padding="12,6"
                               Margin="0,0,8,0"
                               Style="{StaticResource AccentButtonStyle}"/>
                        <Button Content="Delete" 
                               Padding="12,6"
                               IsEnabled="{Binding SelectedProject, Converter={StaticResource NotNullConverter}}"/>
                    </StackPanel>
                    
                    <!-- Projects List -->
                    <ListBox Grid.Row="2" 
                            ItemsSource="{Binding Projects}"
                            SelectedItem="{Binding SelectedProject}"
                            Background="Transparent"
                            BorderThickness="0">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8" 
                                       Background="Transparent">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" 
                                                  FontWeight="SemiBold"
                                                  FontSize="14"
                                                  TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding NoteCount, StringFormat={}{0} notes}" 
                                                  FontSize="12"
                                                  Margin="0,2,0,0"
                                                  Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource DefaultListBoxItemStyle}">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0,2"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                </Grid>
            </Border>
            
            <!-- Notes Panel -->
            <Border Grid.Column="1" 
                   Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                   CornerRadius="8" 
                   Padding="16"
                   BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                   BorderThickness="1">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    
                    <TextBlock Grid.Row="0" 
                              Text="Notes" 
                              FontSize="18" 
                              FontWeight="SemiBold" 
                              Margin="0,0,0,12"/>
                    
                    <!-- Note Actions -->
                    <StackPanel Grid.Row="1" 
                               Orientation="Horizontal" 
                               Margin="0,0,0,12">
                        <Button Content="New Note" 
                               Padding="12,6"
                               Margin="0,0,8,0"
                               Style="{StaticResource AccentButtonStyle}"
                               IsEnabled="{Binding SelectedProject, Converter={StaticResource NotNullConverter}}"/>
                        <Button Content="Edit" 
                               Padding="12,6"
                               Margin="0,0,8,0"
                               IsEnabled="{Binding SelectedNote, Converter={StaticResource NotNullConverter}}"/>
                        <Button Content="Delete" 
                               Padding="12,6"
                               IsEnabled="{Binding SelectedNote, Converter={StaticResource NotNullConverter}}"/>
                    </StackPanel>
                    
                    <!-- Notes List -->
                    <ListBox Grid.Row="2" 
                            ItemsSource="{Binding Notes}"
                            SelectedItem="{Binding SelectedNote}"
                            Background="Transparent"
                            BorderThickness="0"
                            ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Border Padding="8" 
                                       Background="Transparent"
                                       BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                                       BorderThickness="0,0,0,1">
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" 
                                                  FontWeight="SemiBold"
                                                  FontSize="14"
                                                  TextTrimming="CharacterEllipsis"/>
                                        <TextBlock Text="{Binding Excerpt}" 
                                                  FontSize="12"
                                                  Margin="0,4,0,0"
                                                  TextTrimming="CharacterEllipsis"
                                                  TextWrapping="Wrap"
                                                  MaxHeight="40"
                                                  Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                                            <TextBlock Text="{Binding UpdatedAt, StringFormat={}Updated: {0:MMM dd}}" 
                                                      FontSize="10"
                                                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                                            <TextBlock Text="{Binding Tags.Count, StringFormat={} • {0} tags}" 
                                                      FontSize="10"
                                                      Margin="8,0,0,0"
                                                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                        <ListBox.ItemContainerStyle>
                            <Style TargetType="ListBoxItem" BasedOn="{StaticResource DefaultListBoxItemStyle}">
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            </Style>
                        </ListBox.ItemContainerStyle>
                    </ListBox>
                    
                    <!-- Note Editor Placeholder -->
                    <Border Grid.Row="3" 
                           Background="{DynamicResource SystemControlBackgroundAltMediumLowBrush}"
                           CornerRadius="4" 
                           Padding="12" 
                           Margin="0,12,0,0"
                           BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                           BorderThickness="1">
                        <StackPanel>
                            <TextBlock Text="Note Editor" 
                                      FontSize="14" 
                                      FontWeight="SemiBold" 
                                      Margin="0,0,0,8"/>
                            <TextBlock Text="Select a note to edit, or create a new note to get started." 
                                      FontSize="12"
                                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                      TextWrapping="Wrap"/>
                            <TextBlock Text="Markdown editing with live preview will be implemented in future tasks." 
                                      FontSize="11"
                                      Margin="0,8,0,0"
                                      FontStyle="Italic"
                                      Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                                      TextWrapping="Wrap"/>
                        </StackPanel>
                    </Border>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</UserControl>